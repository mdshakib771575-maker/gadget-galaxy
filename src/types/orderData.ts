export interface OrderData {
  productId: string;
  productName: string;
  productImage: string;
  productPrice: number;

  userName: string;
  userEmail: string;

  quantity: number;
  totalPrice: number;
}
